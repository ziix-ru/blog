# 🌐 Ziix.ru Blog

Исходный код личного блога [ziix.ru](https://ziix.ru), построенного на **Hugo** и теме **PaperMod**.
Развертывание осуществляется через Docker + Nginx + Caddy с помощью Ansible.

## 🛠 Технологии

- **Hugo** (Static Site Generator)
- **PaperMod** (Theme)
- **Docker** (Containerization)
- **Nginx** (Web Server for static files)
- **Caddy** (Reverse Proxy & SSL & Matrix Delegation)
- **Ansible** (Deployment automation)

---

## ✍️ Как написать новый пост

1. **Создай файл** в папке `content/posts/`:
   Например: `content/posts/my-new-post.md`

2. **Добавь "шапку" (Frontmatter):**
   В начале файла обязательно укажи метаданные:

```
---
title: "Заголовок поста"
date: 2025-12-07T18:00:00+03:00
draft: false
tags: ["docker", "linux"]
categories: ["tutorial"]
---
```
3. **Напиши контент** ниже шапки, используя Markdown.
---

## 💻 Запуск локально (Проверка)

Чтобы посмотреть сайт перед отправкой на сервер, нужно установить Hugo на компьютер:

```
# MacOS
brew install hugo
# Windows (Chocolatey)
choco install hugo-extended
# Linux (Debian/Ubuntu)
sudo apt install hugo
```

**Запуск сервера:**
```
hugo server -D
```
Сайт будет доступен по адресу: `http://localhost:1313/`
---

## 🚀 Деплой (Как обновить сайт)

Процесс обновления сайта выглядит так:

1. **Сохрани изменения и отправь в GitHub:**
```
git add .
git commit -m "Добавил новый пост про Docker"
git push origin main
```

2. **Запусти Ansible (на управляющей машине):**
```
cd ~/infra
ansible-playbook -i inventory.yml site.yml --tags hugo
```

Ansible автоматически:
1. Стянет обновления из GitHub на сервер.
2. Запустит Docker контейнер с Hugo для сборки сайта.
3. Обновит файлы в папке Nginx.

---

## 📂 Структура проекта

- `content/` — Статьи и страницы.
- `layouts/` — HTML шаблоны (если нужно переопределить тему).
- `static/` — Картинки и файлы (доступны по прямой ссылке /image.png).
- `themes/` — Подмодули тем (PaperMod).
- `hugo.yaml` — Главный конфиг сайта.

<3
